"use client";

export default function Providers({ children }: { children: React.ReactNode }) {
  // NextAuth SessionProvider and other context providers will be added here
  return <>{children}</>;
}
